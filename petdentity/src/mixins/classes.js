// import { computed } from 'vue'

export const customClass = {
  type: String,
  default: ''
}

export const ewan = {
  type: String,
  default: ''
}

// export const classes = computed(() => {
//   let items = []
//   customClass.split(' ').map((c) => items.push(c))
//   return items
// })
